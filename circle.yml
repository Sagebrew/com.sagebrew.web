machine:
  services:
    - rabbitmq-server
    - elasticsearch
  post:
    - celery -A sagebrew worker -l debug -n worker1:
        background: true
    - celery -A sagebrew worker -l debug -n worker2:
        background: true
    - python ~/com.sagebrew.web/sagebrew/manage.py runserver 127.0.0.1:8080:
        background: true


dependencies:
  post:
    - pip install -r ../requirements/web/base.txt
    - pip install git+https://github.com/robinedwards/neomodel.git@0.3.6

checkout:
  post:
    - git submodule sync
    - git submodule update --init

general:
  build_dir: sagebrew

