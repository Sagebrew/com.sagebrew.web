machine:
  environment:
    NEO4J_REST_URL: http://sagebrew_dev:OElnbeUS6I4lyJazdhcc@sagebrewdev.sb02.stations.graphenedb.com:24789/db/data/
    BOMBERMAN_API_KEY: 6a224aea0ecb3601ae9197c5762aef56

dependencies:
  post:
    - pip install -r ../requirements/web/base.txt
    - pip install git+https://github.com/robinedwards/neomodel.git@0.3.6

checkout:
  post:
    - git submodule sync
    - git submodule update --init

general:
  build_dir: sagebrew

