dependencies:
  post:
    - pip install -r ../requirements/web/base.txt
    - export NEO4J_REST_URL=http://sagebrew_dev:OElnbeUS6I4lyJazdhcc@sagebrewdev.sb02.stations.graphenedb.com:24789/db/data/

checkout:
  post:
    - git submodule sync
    - git submodule update --init

general:
  build_dir: sagebrew

