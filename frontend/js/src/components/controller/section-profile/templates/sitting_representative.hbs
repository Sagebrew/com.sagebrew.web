{{#each representative }}
    <small>
        {{# if this.has_website }}
            <a href="{{ this.quest.website }}" target="_blank">
        {{/if}}
        {{ this.first_name }} {{ this.last_name }}
        {{# if this.has_website }}</a>{{/if}}
        <b>&middot;</b> {{ this.terms }} term</small>
    <br />
{{/each}}

